# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/shiv_online_hub
spring.datasource.username=root
spring.datasource.password=Shivapp@1    # Make sure to replace 'yourpassword' with your actual MySQL password

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect  # MySQL 8 Dialect

# JWT Configuration (keep your previous JWT settings)
jwt.secret=shivsecretkey
jwt.expiration=3600000

# Server Configuration
server.port=8080

